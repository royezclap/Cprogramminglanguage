#include <stdio.h>

int main(){
    int n;
    do{
        printf("Enter n: ");
        scanf("%i",&n);
    }while(n>=15);
    int result=1;
    for(int i = 2; i<n; i+=2)
      result*=i;
    printf("%i\n", result);
}
