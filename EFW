#include <stdio.h>
#include <math.h>

int main(){
    int n;
    float e,x;
    
    printf("Enter n, x: ");
    scanf("%i, %f", &n, &x);

    for(int a=1,b=1,e=1;a<=n; a++){
      b=b*a;
      e=e+pow(x,a)/b;
      if(a==n){
        printf("%i \n", e);
        break;
      }
    }
}
